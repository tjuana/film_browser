// Wishlist route - no loader needed as it uses Zustand store
// This file exists for consistency and future enhancements

export type WishlistLoaderData = undefined;

// No loader function - wishlist data comes from Zustand store
// The WishListPage component will continue to use useWishlistSelectors()
